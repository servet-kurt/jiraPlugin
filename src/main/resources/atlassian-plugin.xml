<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
    </plugin-info>
    
    
    <customfield-type key="issuemonitor" name="Issue Monitor" class="com.atlas.plugin.cf.NewCustomField">
    <description>A custom field to Monitor Issues</description>
    <resource type="velocity" name="view" location="templates/plugins/fields/view/view-basictext.vm"/>
    
    
</customfield-type>
</atlassian-plugin>